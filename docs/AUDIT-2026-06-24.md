# AUDIT d'écart - Blindify web vs Definition of Done

> Écrit le 2026-06-24. Audit en lecture seule, aucun code modifié, aucun build, aucun E2E lancé.
> Cible : les 4 points du Definition of Done (HANDOFF.md §7). Mesure l'écart, ne construit rien.

## Résumé en une ligne

Le cœur du jeu tourne. Sur les 4 points : aucun n'est bloqué côté serveur, 1 reste à prouver (8 joueurs), 1 n'existe pas (signalement bug), 1 est à reskin (logo), 1 demande une vraie passe (mobile). Aucun chantier ne touche la machine à états fragile si on reste discipliné.

---

## Point 1 - Deux modes principaux à 8 joueurs

**Verdict : techniquement permis, JAMAIS prouvé à 8.**

- Backend : cap à 16, défaut 12, plancher 2. `backend/src/controllers/roomsController.ts:147` (`min(max(n,2),16)`). 8 est largement dans la marge, aucun changement de cap nécessaire.
- Frontend friends : défaut 8 si `room.max_players` null, grille `sm:grid-cols-2`. Tient à 8. `frontend/src/app/multiplayer/FriendsLobbyView.tsx:327`.
- Event : pas de grille de slots (juste un compteur), aucun risque de layout.
- Classement / reveal : liste flex verticale, `max-width:460px`, scale sans casser à 8. `TheaterGameView.tsx` (finale-board).

**L'écart est de la PREUVE, pas du code.** Les E2E montent à 2 joueurs max (`multiplayer-2players`, `event-2players`, `multiplayer-full-game`, `multiplayer-sync`). Le seuil de 8 et la perf socket à 8 clients simultanés ne sont pas vérifiés.

**À faire** : valider une vraie partie à 8 (les deux modes) + un E2E qui monte à 8. Effort faible-moyen, gros gain de confiance.

---

## Point 2 - Canal de signalement de bug in-app

**Verdict : n'existe pas. Confirmé.**

- Aucun composant frontend (modal/bouton "signaler/feedback/bug/support").
- Aucune route backend (`routes/` : auth, games, likes, rooms, stats, audioSources, friends, invitations, import, quickPlay, challenges. Pas de feedback).
- Aucune table DB (`schema.sql`, migrations : rien pour feedback/bug).
- Aucune lib email côté blindify (pas de Resend/nodemailer dans `backend/package.json`).

**Briques déjà là pour le construire** :
- Sentry : placeholder `SENTRY_DSN` dans `.env.example:29`, non câblé (capture passive, pas un canal joueur).
- Pattern réutilisable sur le VPS : `/opt/clearpath-feedback` (Resend + table `feedback_responses` + route `/api/feedback`).
- Postgres opérationnel : ajouter une table `bug_reports` est trivial.

**À faire** : construire le canal. Décision de forme requise (voir Décisions). Effort faible-moyen, self-contained, risque bas.

---

## Point 3 - Un logo

**Verdict : un logo existe mais ne colle pas à la DA. À reskin.**

- `frontend/public/logo_blindify.png` : style néon (dégradé orange/magenta/bleu, casque + waveform). Incompatible avec la DA Club analogique (terracotta/or/sauge/papier). Tous les favicons en héritent (favicon-16/32, apple-touch, icon-192/512).
- `frontend/src/components/Logo.tsx` : `rounded-2xl` + `text-white`, tous deux hors DA (le guide interdit `text-white`).
- Surfaces mortes qui l'affichent encore :
  - `components/ui/navbar.tsx` : orpheline, ancienne DA sombre (`bg-background/80 backdrop-blur-xl`, gradient). N'est importée par aucun layout.
  - `app/auth/login/page.tsx` : auth abandonnée (remplacée par le wizard nom+musique).
- Surfaces vivantes OK (conversion analog en cours) : `landing/navigation.tsx`, `stats/page.tsx`.

**À faire** : un logo qui respecte la DA actuelle (probablement reskin de l'image + fix du composant + retrait des surfaces mortes), pas un from scratch. Périmètre à confirmer (voir Décisions). Risque technique bas.

---

## Point 4 - App adaptée au téléphone

**Verdict : pas fini. Plusieurs correctifs ciblés, surtout sur l'écran de jeu mobile (`TheaterGameView.tsx`).**

Faux positif écarté : le `<meta viewport>` EST présent dans le HTML de prod (Next.js l'injecte, vérifié dans `out/index.html`). Ce n'est pas un problème.

Problèmes réels prouvés dans le code (à confirmer sur device réel, l'analyse est statique) :

1. **Grille de jeu à colonne fixe.** `TheaterGameView.tsx:182` : `gridTemplateColumns: "1fr 320px"` sans breakpoint propre. La correction mobile est à `@media (max-width:900px)`, pas alignée sur 640px. Risque : à 375px le classement latéral écrase la zone de jeu si la media query ne mord pas comme prévu.
2. **Drawer de chat fixe 340px.** `TheaterGameView.tsx:1141` : `position:fixed; width:340px`. Sur 375px, chat ouvert = jeu quasi inaccessible. Pas de passage plein largeur sur mobile.
3. **Cibles tactiles du picker "Qui a ajouté" trop petites.** `TheaterGameView.tsx:1017` : avatars 26px, hauteur de bouton ~20-24px, bien sous les 44px requis. Erreurs de sélection au pouce probables.
4. **Breakpoints désynchronisés** (640px vs 900px) : états intermédiaires (tablette portrait, chat ouvert) mal gérés.

Plus léger : QR fixe 168px en event (tient mais serré), quelques `text-[9px]/[10px]` sur labels, `whitespace-nowrap` sur noms longs (masqué par overflow-hidden, pas de scroll horizontal réel).

**À faire** : passe mobile sur le parcours des 2 modes (wizard -> lobby -> jeu -> reveal -> résultats). C'est le plus gros morceau et le plus proche de l'usage réel. Tout changement sur `TheaterGameView`/`MultiplayerGameClient` exige un E2E 2 joueurs derrière (zone fragile, cf. PITFALLS §2).

---

## Backlog ordonné (par valeur et par risque) - proposition pour demain

0. **Filet de sécurité d'abord** : lancer l'E2E 2 joueurs pour confirmer que le lot QoL n'a rien cassé dans la boucle de jeu (modif récente de `MultiplayerGameClient.tsx`, jamais rejoué depuis). Prévenir avant : l'E2E tape la prod `tymmerc.eu/blindify`, à faire hors heures de jeu.
1. **Point 1 (8 joueurs)** : vérifier + pousser à 8 sur les 2 modes, ajouter un E2E 8 joueurs. Cheap, gros gain de confiance, débloque officiellement un des 4 points.
2. **Point 4 (mobile)** : la passe la plus lourde et la plus proche de l'usage réel. Découper par écran, E2E derrière chaque touche au game screen.
3. **Point 2 (canal bug)** : self-contained, faible risque, pattern `clearpath-feedback` réutilisable. Peut avancer en parallèle du mobile.
4. **Point 3 (logo)** : dépend d'une décision design, risque technique bas, à caler quand le périmètre est tranché.

## Décisions produit prises (à appliquer)

- **Nommage des modes** (user, 2026-06-24) : mode 1 (friends) = **"À distance"** (avant : "Entre amis, à distance"), mode 2 (event) = **"Autour d'une table"** (inchangé), mode 3 (streamer) = **"Avec ta communauté"**. Mode 1 APPLIQUÉ dans le code le 2026-06-24 (modes/page.tsx: titre de carte + libellé recap, mode 1 = "À distance"). Modes 2 et 3 avaient déjà le bon libellé. Pas encore déployé en prod (attente GO). Rappel : le mode 3 reste hors release web (WIP), c'est juste son nom qui est fixé.

## Décisions qui te reviennent (proposées, pas tranchées)

- **8 joueurs** : on vérifie juste que 8 marche, ou on monte le défaut visible à 8 partout ? (backend défaut = 12, friends lobby tombe à 8 si null)
- **Canal bug** : forme ? Simple table DB + une vue admin, ou email (Resend, comme clearpath) vers ta boîte, ou les deux ? Et le rapport part où ?
- **Logo** : on redessine l'image pour qu'elle colle à la DA analog, ou on se contente de fixer le composant + le sortir des surfaces mortes ? From scratch non souhaité a priori.
- **E2E sur prod** : quel créneau ? Ça joue de vraies parties sur le domaine live.

## Filet de sécurité E2E - résultat (2026-06-24)

E2E `multiplayer-2players.spec.ts` lancé sur la prod : **1 passé / 1 ÉCHOUÉ**.

- Test KO : "full game: launch, play 2 rounds, verify round advancement". Après le clic "Lancer", l'UI de jeu (`.theater-round`) n'apparaît jamais.
- Cause localisée via logs serveur : le `POST /blindify/api/rooms/<code>/start` renvoie **400** (rejeté par le backend). Du coup aucun round ne démarre, aucun `.theater-round` rendu.
- Gardes 400 candidates dans `roomsController.startGame` : "playlist_provider_invalid" / "provider_connection_missing" ("Connexion Spotify requise"). Le host du test importe via un profil Spotify SYNTHÉTIQUE (`open.spotify.com/user/yigiha...`) sans connexion OAuth réelle.
- **PAS causé par le renommage + fix tests audio** (qui ne touchent ni le launch ni la boucle de jeu). Condition préexistante, héritée du lot QoL de la session précédente jamais retesté.

**Ambiguïté NON tranchée** : artefact de test (compte synthétique) VS vrai bug. Risque réel : avec le nouveau wizard, un vrai user colle un lien de profil Spotify ; s'il faut une connexion OAuth pour démarrer, il prend le même 400. À CONFIRMER par une vraie partie 2 joueurs (compte réel / depuis le téléphone), impossible à reproduire en headless avec audio.

**Reco** : confirmer la santé réelle de la prod (vraie partie) AVANT tout chantier mobile/8 joueurs, qui dépendent d'une boucle de jeu saine. Si bug réel sur le start avec profil collé -> priorité 1, à investiguer sur un build dev instrumenté (pas en aveugle sur la prod).

### Résolution (2026-06-24, même jour)

**Tranché : ARTEFACT DE TEST, pas un bug prod.** Preuves DB :
- Users de test (GameHost 2272, Player2 2273) : **0 piste** dans `audio_sources`.
- Global prod : **4529 sources réparties sur 15 users réels**. Base saine.
- Le 400 = garde `need_more_music` (`roomsController.ts:619`, "Il faut au moins 2 joueurs ayant importé leur musique"). Le test importe un profil Spotify synthétique (0 piste, OAuth requis) et Player2 n'importe rien -> moins de 2 joueurs avec musique -> refus légitime.

**Conséquence** : le E2E `multiplayer-2players` meurt à la précondition musique, donc il **ne teste jamais la vraie boucle de jeu** (launch -> rounds -> reveal). Ce n'est pas un filet de sécurité utile en l'état.

**À faire pour en faire un vrai filet** : seed de vraies pistes jouables (Deezer, pas d'OAuth) pour les 2 users de test avant le launch, OU import réel via le flux Deezer. Ensuite le test exerce vraiment la boucle. Tant que ce n'est pas fait, on n'a pas de validation E2E automatique de la boucle de jeu.

## Décision d'archi data (2026-06-24, validée user)

**Direction validée : "stocker le lien, résoudre à la volée".**
- On garde le user (nom + lien de playlist/profil) pour le restore au retour.
- On NE stocke PLUS le catalogue de titres dans `audio_sources` (aujourd'hui 4529 lignes, avec un `UNIQUE(provider, external_id)` qui force un seul propriétaire par titre = smell).
- On stocke la référence (provider + lien + propriétaire), et on résout titres + previews à la volée au lancement, avec cache court (Redis) le temps de la partie.
- Bénéfice clé : chaque plateforme devient un simple resolver, le multi-plateforme est bien plus simple. À faire AVANT/AVEC l'ajout de plateformes.
- Risques à gérer : latence + rate-limit API au lancement, résilience si l'API tombe (cache Redis mitige).

## Backlog ajouté (user, 2026-06-24)

- **Accompagnement utilisateur pour l'import du lien.** UX complète pour aider le joueur à coller son lien de playlist/profil :
  - Instructions claires par plateforme (où trouver le lien public Spotify/Deezer, comment le copier).
  - Validation + messages d'erreur explicites pour les cas réels découverts au test : playlist privée, playlist éditoriale Spotify (bloquée, 404), URL non reconnue, playlist vide.
  - Feedback de progression pendant l'import (nb de titres récupérés), état de succès.
  - À étendre à chaque nouvelle plateforme ajoutée.

## Audit faisabilité multi-plateformes (2026-06-24)

**Enabler clé :** l'app résout TOUTES les previews via Deezer (recherche titre+artiste), quelle que soit la plateforme source. Donc une nouvelle plateforme n'a qu'à fournir une **liste (titre, artiste)** d'une playlist/profil public. Pas besoin que la plateforme expose de l'audio. Ça allège énormément.

**Points d'ancrage (localisés, pas de refonte) :** `backend/src/services/profileImportService.ts` → `parseProfileUrl` (ajouter le host), `fetchPublicPlaylists` + `fetchPlaylistTracks` (ajouter un fetcher). Le type `MusicProvider` (`backend/src/types/user.ts`) inclut déjà "apple". Frontend : instructions de lien + icône par plateforme. Une page `settings/providers` mentionne déjà Apple Music (stub).

**Par plateforme :**
- **Apple Music** : FAISABLE (effort moyen). API officielle pour lister une playlist publique. Coût : compte Apple Developer (99$/an) + token JWT (MusicKit). Métadonnées propres. Meilleur candidat.
- **YouTube / YT Music** : FAISABLE mais bancal. YouTube Data API (gratuit, quota) liste les items, mais ce sont des TITRES DE VIDÉO ("Artiste - Titre (Official Video)"), pas du titre+artiste propre. Nécessite une heuristique de nettoyage, taux de match plus faible. YT Music n'a pas d'API officielle (libs non officielles fragiles).
- **SoundCloud** : NON FAISABLE aujourd'hui (enregistrement d'app fermé depuis des années, pas de clé API).
- **Tidal / Amazon Music** : NON (API partenaire/restreinte).

**Reco :** Apple Music en priorité (qualité + API officielle), YouTube en "best effort" optionnel. Faire la refonte data "stocke le lien" d'abord ou avec la 1re nouvelle plateforme, pour poser le pattern resolver proprement.

**À surveiller (Spotify, déjà intégré) :** playlists éditoriales 404, et `preview_url` Spotify de plus en plus null. L'app contourne déjà via Deezer (bien), mais dépend de l'accès client-credentials de Spotify pour lister les titres, que Spotify resserre.

### Clarification modèle d'import (user, 2026-06-24)

Le modèle = **copier-coller d'un lien de profil/playlist PUBLIC, sans connexion/OAuth de l'utilisateur** (prouvé au test 4 joueurs : zéro login). L'accompagnement utilisateur porte sur "où trouver et copier mon lien public", pas sur une connexion de compte.

Précision sur le coût Apple Music : le 99$/an N'EST PAS une connexion utilisateur. C'est l'accès API côté APP :
- Deezer = API 100% ouverte (aucune clé).
- Spotify = token d'app gratuit (client-credentials).
- Apple Music = aucun accès sans token développeur signé => compte Apple Developer payant (99$/an), même pour lire un lien public.
Côté UX c'est identique (coller un lien) ; le blocage Apple est purement l'accès API de l'app. YouTube Data API = gratuite (clé API, pas de paiement) mais métadonnées de titres de vidéo à nettoyer.

## Preview DEV de l'app (monté 2026-06-24)

But : voir/valider les modifs UI sur `dev.tymmerc.eu` AVANT de builder en prod (build prod = déploiement instantané).
- Serveur : `next dev` du working tree `/opt/blindify/frontend` sur le port **3099** (HMR live, mes edits src s'affichent direct). Lancé en tâche de fond.
- nginx (`/etc/nginx/sites-enabled/12-dev-https.conf`) : `^~ /blindify/` -> proxy 127.0.0.1:3099 ; `^~ /blindify/api/` et `^~ /blindify/socket.io/` -> `blindify_backend`, avec `proxy_set_header Origin https://tymmerc.eu` (le backend n'autorise que tymmerc.eu, allowedOrigins index.ts:77 ; le rewrite contourne sans toucher au backend).
- URL : **https://dev.tymmerc.eu/blindify/**
- Le dev tape le backend/DB de PROD (ok pour review visuelle ; pollue un peu la DB en parties de test).
- Relancer si besoin : `cd /opt/blindify/frontend && unset __NEXT_PRIVATE_STANDALONE_CONFIG NODE_ENV && PATH="./.node/bin:$PATH" npx next dev -p 3099` (en fond).
